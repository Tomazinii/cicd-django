name: Django CI

on:
  push:
    branches: [ "main" ]

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     max-parallel: 4
  #     matrix:
  #       python-version: [3.8]

  #   steps:
  #   - uses: actions/checkout@v3
  #   - name: Set up Python ${{ matrix.python-version }}
  #     uses: actions/setup-python@v3
  #     with:
  #       python-version: ${{ matrix.python-version }}
  #   - name: Install Dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -r requirements.txt
  #   - name: Run Tests
  #     run: |
  #       pytest
  build:
    runs-on: ubuntu-latest  # Escolha o sistema operacional adequado

    steps:
    - name: Checkout do código
      uses: actions/checkout@v2  # Para obter o código do repositório

    - name: SSH para o servidor e execute o comando
      run: |
        ssh alotropo@192.168.15.4 'echo "fazendo um teste"'

      env:
        SSH_AUTH_SOCK: /tmp/ssh_agent.sock
      # Configure as credenciais do SSH
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}


